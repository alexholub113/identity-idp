# Development environment configuration
API_BASE_URL=https://localhost:57480

# You can also create .env.local for local overrides (this file is gitignored by default)
# For production, set this in your deployment environment
